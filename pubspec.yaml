name: stuff
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.9.0 # Update to the latest version
  firebase_auth: ^5.3.4  # Update to the latest version
  cloud_firestore: ^5.0.0 # Update to the latest version
  multi_select_flutter: ^4.0.0
  flutter_launcher_icons: ^0.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  assets:
    - assets/background.png
    - assets/stuff_button.jpeg
    - assets/thc.png
    - assets/icon/icon.png
  uses-material-design: true

